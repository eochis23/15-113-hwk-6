
* **Model:** Gemini 3.1 Pro 
* **Libraries/Tools:** Python, SQLite3, gspread, Google Colab Auth, Google Drive API, pandas, python-Levenshtein, deepcsim

* "can you give the whole cell for the uncorrected cells again and make sure it can find files with a path like that? it failed to find any before"
* "The path for uncorrected files is predictable. It'll always have a format like the one I provided above. Use that"
* "--> 105         cur.executemany("""    106         INSERT OR REPLACE INTO submissions (    107             wsNum, problemName, version, problemPoints, andrewID, OperationalError: table submissions has no column named version"
* "just give me the new first cell like it was before but correct with version added. I'll delete the db myself"
* "split the cell so the table creation and population are separate cells"
* "write a quick test cell to print all submissions"
* "in the cell asking for input, keep asking until the user says "stop""
* "In this conversation, unless I say otherwise you should provide a full cell, but only the cell you changed"
* "now let's do a function for adding corrected submissions. You should search the spreadsheet and add them directly to the db (in the corrected column in the already existing entry, replacing the current value there) instead of creating files for them. Do you know how to do this? ask questions if needed"
* "how does the current script do it? the tabs are called ws[num]. [Provided previous apps script payload code]"
* "There is some raw data to the left and then the headers are like this: andrewIDgetKthDigitAdotsOverlapAisDuplicatedNumberAhowManyPizzasBsetKthDigitBisDuplicatedNumberBgetGreenCclosestEvenCisDuplicatedNumberC so you should find andrewID as the left and then to the right the columns are the problem name concatenated with the single letter version"
* "If there isn't uncorrected code, can you throw an error?"
* "Actually, catch that and ask the user if they'd like to move on in each of those cases. If that happens, then make the uncorrected code an empty stringActually, catch that and ask the user if they'd like to move on in each of those cases. If that happens, then make the uncorrected code the string "NO UNCORRECTED CODE""
* "Make the loop structure of this function the same as the first one"
* "yes. Make a new cell with these functions (modified to take in strings instead of paths) to generate the similarity score and notes: [Provided cleanCode, compareCode, and Levenshtein code snippets]"
* "it's from deepcsim import compare_source"
* "partial credit starts as:         partialCreditPct    REAL DEFAULT 0.0, and I want it to stay between 0 and 100"
* "make the similarity score generation run in a command line interface like the others"
* "can you do that like this? [Provided target_folders and gspread processing snippet]"
* "let's do an interactive loop again"
* "don't remove unused tabs"
* "If a student has no original submission, then don't add them to the sheet"
* "combine all of these functions into one loop with one cli. It should be structured like menus where the user selects the operation to perform, then the options from those currently listed"
* "add one last menu where we can view and delete specific student submissions (first go into that menu, then ask for their andrewID, then narrow it down with the ws num, then give options for the problems, then show the code and give options to go back or delete it)"
* "stay in a menu unless I choose to go back"
* "remember, clear the tab in the sheet when exporting and don't export where the partial credit percent is 0remember, clear the tab in the sheet when exporting and don't export where students don't have partial credit submissions"
